# D-011 spec: 確定要件(D-010 の回答)

田淵さんからの回答(早瀬経由)。**これが確定要件です。**

| # | 確認事項 | 回答 |
|---|---|---|
| 1 | タグは誰が付ける? | **client が案件登録(S-12)のときに付ける** |
| 2 | 1案件にいくつまで? | **最大3つ**(0個でもよい) |
| 3 | タグの選択肢は誰が管理する? | **運営が用意した一覧から選ぶ**(自由入力はしない)。当面は「PHP / SQL / 短期 / 長期 / 未経験歓迎」の5つで開始。追加は運営への依頼ベース(管理画面は作らない) |
| 4 | 「リモート」もタグにする? | **しない。** リモートは既存の「リモート可」チェック(is_remote)のままでよい |
| 5 | 探す側はどう使う? | **engineer が案件一覧(S-03)の検索条件でタグを1つ選んで絞る**(未選択なら絞らない) |
| 6 | 付け間違いの修正は? | **今回はやらない。** 案件の編集画面自体がまだ無いため、次の相談に回す(D-010 の切り分けどおり) |

## 基本設計で決めること(この課題の範囲)

- F-20 として: 概要/誰が・いつ付けるか/誰が・どこで使うか/**やらないこと**
- 画面側(screens.md): S-03(検索条件に追加)・S-04(表示に追加)・S-12(入力に追加)の各1行
- 画面遷移図(screens.md 4節の Mermaid): タグで絞り込む流れを一言追記。**新しい画面は増えない**ので、
  既存の S-03(alias: PI)に「タグで絞り込み」の矢印を1本足すだけで十分(例: `PI -->|タグで絞り込み| PI`
  のような自己ループ、または同じ意味を表す別の書き方でも可)

## 基本設計で決めないこと(次の工程に渡す)

- テーブル・カラムの定義 → **D-012(DB設計)**
- 入力検証の詳細・エラーメッセージの文言・URL パラメータ → **D-013(詳細設計)**

この線引き自体が、この課題で学ぶことです。
