# T-015 rubric

- [ ] `companies` と `projects` を JOIN した
- [ ] `projects.status = 'open'` の案件だけを数えた
- [ ] `GROUP BY` で企業ごとにまとめた
- [ ] `COUNT(*)` に `open_project_count` という別名を付けた
- [ ] 件数が多い順、同数なら企業名順に並べた
