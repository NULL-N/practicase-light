# logs/ — アプリの動作ログの置き場

アプリを動かすと、ここに `app.log` が生成されます(`App\Support\Logger`)。

- **`app.log` は git 管理しません**(.gitignore 済み — 環境ごとの実行記録のため)
- 書式・規約は `docs/02_作業ルール/coding-rules.md` の **LOG 節**を参照
- 障害調査の課題では、このログが一次資料になります
- 消してしまっても問題ありません(次のアクセスから再生成されます)
