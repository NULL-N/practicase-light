{
    // フォルダを開いたとき、タスクやターミナルを使わず VS Code 純正の機能で README.md の
    // プレビュー(整形表示)を自動で開く。README には T-000 から始める案内が書かれている。
    // 2度目以降の起動では前回開いていたタブを復元する(VS Code 標準の挙動。毎回は出ない)。
    "workbench.startupEditor": "readme"
}
